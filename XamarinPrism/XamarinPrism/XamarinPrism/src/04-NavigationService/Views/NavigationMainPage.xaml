<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamarinPrism.src._04_NavigationService.Views.NavigationMainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Name" />
            <Entry Placeholder="Enter name" Text="{Binding Character.Name}" />
            <Label Text="Age" />
            <Entry Placeholder="Enter age" Text="{Binding Character.Age}" />
            <Label Text="BirthDay" />
            <DatePicker Date="{Binding Character.BirthDay}" />
            <Button
                Command="{Binding OnSendChacterCommand}"
                CommandParameter="{Binding Character}"
                Text="Send The Fist Page" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>