<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamarinPrism.src._13_MessageCenter.Views.MessageAPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ContentPage.Content>
        <StackLayout>
            <!--<Switch IsToggled="{Binding IsToggled, Mode=TwoWay}">
                <Switch.Behaviors>
                    <prism:EventToCommandBehavior Command="{Binding OnToggleCommand}" EventName="Toggled" />
                </Switch.Behaviors>
            </Switch>-->

            <Button Command="{Binding OnSubscribeCommand}" Text="Subscribe" />
            <Button Command="{Binding OnNavigationCommand}" Text="Go to view B" />
            <Button Command="{Binding OnUnsubscribeCommand}" Text="Unsubscribe" />
            <ListView ItemsSource="{Binding Collection}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>