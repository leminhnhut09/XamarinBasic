<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage
    x:Class="PrismXamarin.src._08_MasterDetailPage.Views.PrismMasterDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">

    <MasterDetailPage.Master>
        <ContentPage
            Title="Menu"
            Padding="{OnPlatform Default='20',
                                 iOS='20,40'}"
            IconImageSource="{OnPlatform iOS='hamburger.png'}">
            <StackLayout>
                <Label
                    Margin="0,20"
                    FontAttributes="Bold"
                    FontSize="Large"
                    Text="Master Detail Demo" />
                <Button
                    Command="{Binding NavigateCommand}"
                    CommandParameter="ViewA"
                    Text="View A" />
                <Button
                    Command="{Binding NavigateCommand}"
                    CommandParameter="ViewB"
                    Text="View B" />
                <Button
                    Command="{Binding NavigateCommand}"
                    CommandParameter="TabbedPage?createTab=ViewA&amp;createTab=ViewB"
                    Text="TabbedPage" />

            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>

</MasterDetailPage>