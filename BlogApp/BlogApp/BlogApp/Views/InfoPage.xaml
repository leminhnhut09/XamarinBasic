<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BlogApp.Views.InfoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{DynamicResource TertiaryTextColor}">
    <ContentPage.Content>
        <StackLayout
            Padding="10,0"
            HorizontalOptions="FillAndExpand"
            VerticalOptions="CenterAndExpand">
            <Image
                Aspect="AspectFill"
                BackgroundColor="Red"
                HeightRequest="200"
                HorizontalOptions="Center"
                Source="image.png"
                VerticalOptions="Center"
                WidthRequest="200">
                <Image.Clip>
                    <EllipseGeometry
                        Center="100,100"
                        RadiusX="100"
                        RadiusY="100" />
                </Image.Clip>
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OnNavigationCommand}" />
                </Image.GestureRecognizers>
            </Image>

            <Entry
                Margin="0,40,0,0"
                Style="{DynamicResource placeColorStyle}"
                Text="Lê Minh Nhựt" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>