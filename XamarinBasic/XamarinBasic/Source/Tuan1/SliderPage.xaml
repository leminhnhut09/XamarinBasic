<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamarinBasic.SliderPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Slider">
    <ContentPage.Content>
        <StackLayout x:Name="layoutMain" Padding="20">
            <Slider
                x:Name="sliderAngle"
                BackgroundColor="Red"
                Maximum="360"
                Minimum="0" />
            <Label FontSize="20" Text="Value" />
            <Label Text="{Binding Source={x:Reference sliderAngle}, Path=Value}" />
            <Label FontSize="20" Text="Editor" />
            <Editor
                x:Name="editorDescription"
                BackgroundColor="LightBlue"
                FontAttributes="Italic"
                FontSize="16"
                HeightRequest="100"
                IsSpellCheckEnabled="True"
                IsTextPredictionEnabled="True"
                MaxLength="1000"
                TextChanged="EditorDescription_TextChanged" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>