<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamarinBasic.ListViewPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="List View">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <ListView x:Name="lvListView" ItemsSource="{Binding Collection}" ItemSelected="lvListView_ItemSelected">
                <ListView.Footer>
                    <Button
                        x:Name="btnClick"
                        Clicked="btnRemove_Clicked"
                        Text="Remove" />
                </ListView.Footer>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding IdProduct}" VerticalTextAlignment="Center" />
                                <Label
                                    Text="{Binding NameProduct}"
                                    TextColor="{Binding Price}"
                                    VerticalTextAlignment="Center" />
                                <Label Text="{Binding Price}" VerticalTextAlignment="Center" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>